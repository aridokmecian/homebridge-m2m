{
    "pluginAlias": "M2M",
    "pluginType": "platform",
    "singular": false,
    "schema": {
        "type": "object",
        "properties": {
            "name": {
                "title": "Name",
                "type": "string",
                "default": "Alarm",
                "required": true
            },
            "username": {
                "title": "Username",
                "type": "string",
                "required": true,
                "description": "Username used when logging into your RControl or DSC Connect account."
            },
            "password": {
                "title": "Password",
                "type": "string",
                "required": true,
                "description": "Password used when logging into your RControl or DSC Connect account."
            },
            "enableZoneSensors": {
                "title": "Enable Zone Sensors",
                "type": "boolean",
                "default": false,
                "description": "Register each alarm zone as a HomeKit contact/motion sensor."
            },
            "imei": {
                "title": "Panel IMEI",
                "type": "string",
                "required": false,
                "description": "If your account has more than one panel registered, the IMEI of the one this accessory should control. Leave blank to use the first panel on the account."
            },
            "partitionNumber": {
                "title": "Partition Number",
                "type": "string",
                "required": false,
                "description": "If your panel has more than one partition, the partition this accessory should control. Leave blank to use the first partition on the panel."
            },
            "pollingIntervalSeconds": {
                "title": "Zone Polling Interval (seconds)",
                "type": "integer",
                "default": 10,
                "minimum": 5,
                "description": "How often to check for zone state changes when zone sensors are enabled."
            }
        }
    },
    "form": [
        "name",
        "username",
        "password",
        "enableZoneSensors",
        {
            "type": "fieldset",
            "title": "Advanced Settings",
            "expandable": true,
            "expanded": false,
            "items": [
                "imei",
                "partitionNumber",
                "pollingIntervalSeconds"
            ]
        }
    ],
    "display": null
}
